<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network access to backend -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Classic Bluetooth (for HPRT printer) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <!-- Android 12+ Bluetooth permissions -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_CONNECT"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Screen 1 – Connection Setup (Launcher) -->
        <activity
            android:name=".ui.connection.ConnectionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Screen 2 – Location Selector -->
        <activity
            android:name=".ui.location.LocationSelectorActivity"
            android:exported="false" />

        <!-- Screen 3 – Room Selector -->
        <activity
            android:name=".ui.room.RoomSelectorActivity"
            android:exported="false" />

        <!-- Screen 4 – Inventory Checklist -->
        <activity
            android:name=".ui.inventory.InventoryActivity"
            android:exported="false" />

        <!-- Screen 5 – Asset Detailed View -->
        <activity
            android:name=".ui.asset.AssetDetailActivity"
            android:exported="false" />


    </application>

</manifest>
